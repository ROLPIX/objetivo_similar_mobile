rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODO DEMO: Permissões simplificadas para desenvolvimento e validação rápida
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
